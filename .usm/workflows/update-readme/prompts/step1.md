Ensure the @README.md file is uptodate with the latest available commands. 

Use ./usm --help to navigate the available commands. 
Once got the subcommands, use another single huge command to inspect all the nested subcommands so that I can speed up the process
